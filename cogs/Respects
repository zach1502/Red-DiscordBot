import asyncio
from discord.ext import commands
import os

class Respects:
    def __init__(self, bot):
        self.bot = bot
    @commands.command(name="f", pass_context=True)
    async def mycom(self, context):
        await bot.say(context.message.author + "hAs pAiD rEsPeCtS")
        os.fork()
